# madmom venv — beat detection (RNN+DBN) + secondary chord detector
#
# NumPy must be <2.0: madmom's Cython extensions reference np.int/np.float
# which were removed in NumPy 2.0.  We handle this at runtime with a
# monkey-patch, but the extensions must still *compile* against NumPy <2.0.
#
# Install order matters: numpy and Cython must be present before madmom
# so its setup.py can compile the extensions.  setup.sh handles this.

numpy>=1.20,<2.0
Cython
scipy
mido
# Audio I/O (used by both madmom internals and our helper scripts)
soundfile
pydub
librosa
