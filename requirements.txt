# Beat stabilizer dependencies — install with system Python (python3)
# Usage: pip install -r requirements.txt
#
# System prereqs (macOS):  brew install rubberband ffmpeg
# System prereqs (Linux):  sudo apt install rubberband-cli ffmpeg

librosa>=0.10.0
soundfile>=0.12.1
pyrubberband>=0.3.0
pydub>=0.25.1
numpy>=1.24.0
